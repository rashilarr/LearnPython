# f string - format

a = 8
b = 9

x = f'seven {a} {b}'   #fstring is availabe from python version 3.6
                       # f string is used instead of .format(a,b) and a and b is used inside the braces
print(f'x is {x}')

